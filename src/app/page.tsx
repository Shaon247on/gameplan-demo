import { Button } from "@/components/ui/button";


export default function Home() {
  return (
    <div>
      Hello world
      <Button variant={"primary"}>Tap tap</Button>
      <Button variant={"optional"}>Tap tap</Button>
      <Button variant={"default"}>Tap tap</Button>
    </div>
  );
}
